export enum GameModes {
  ONE = "Game Mode 1",
  TWO = "Game Mode 2",
}

export enum Status {
  WIN = "win",
  INCORRECT = "incorrect",
  TIMEOUT = "timeout",
}

export enum AvailableCharacters {
  HIRAGANA = "Hiragana",
  CYRILLIC_RU = "Cyrillic(RU)",
}
