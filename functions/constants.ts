export enum GameModes {
  ONE = "1",
  TWO = "2",
}

export enum Status {
  WIN = "win",
  INCORRECT = "incorrect",
  TIMEOUT = "timeout",
}

export enum AvailableCharacters {
  HIRAGANA = "Hiragana",
  CYRILLIC_RU = "Cyrillic(RU)",
}
